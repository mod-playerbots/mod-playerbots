name: windows-build
on:
  push:
    branches: [ "master", "test-staging" ]
  pull_request:
    branches: [ "master", "test-staging" ]

concurrency:
  group: "windows-build-${{ github.event.pull_request.number }}"
  cancel-in-progress: true

jobs:
  windows-build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest]
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.os }}
    env:
      BOOST_ROOT: C:\local\boost_1_82_0

    steps:
      - name: Checkout AzerothCore
        uses: actions/checkout@v3
        with:
          repository: 'mod-playerbots/azerothcore-wotlk'
          ref: ${{ (github.base_ref || github.ref_name) == 'test-staging' && 'test-staging' || 'Playerbot' }}
          path: 'ac'

      - name: Checkout Playerbot Module
        uses: actions/checkout@v3
        with:
          repository: 'mod-playerbots/mod-playerbots'
          path: ac/modules/mod-playerbots

      - name: Install Ninja
        shell: powershell
        run: |
          choco install ninja -y

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2.13

      - name: Configure OS
        shell: powershell
        working-directory: ac
        env:
          CONTINUOUS_INTEGRATION: true
        run: |
          bash ./acore.sh install-deps

      - name: Build (Ninja)
        shell: powershell
        working-directory: ac
        env:
          CMAKE_GENERATOR: Ninja
        run: |
          # Set MSVC environment then run acore.sh under bash so acore.sh behaves as expected
          $vcvars = "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          if (-Not (Test-Path $vcvars)) {
            $vcvars = "C:\Program Files (x86)\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          }
          if (-Not (Test-Path $vcvars)) {
            Write-Error "vcvars64.bat not found. Ensure Visual Studio 2022 is installed on the runner."
            exit 1
          }
          # Call vcvars to populate MSVC env, then run bash with that environment
          cmd.exe /c "`"$vcvars`" && bash -lc 'export CTOOLS_BUILD=all; export CMAKE_GENERATOR=\"Ninja\"; ./acore.sh compiler build'"

